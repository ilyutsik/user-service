name: Gradle CI/CD Pipeline

on:
  push:
    branches: ["tasks/USR-0", "main"]
  pull_request:
    branches: ["tasks/USR-0", "main"]

jobs:
  ci-build-test-analysis-upload:
    name: CI - Build, Test, SonarQube Analysis, JAR Upload
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: corretto
          java-version: 21
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle') }}
          restore-keys: ${{ runner.os }}-gradle

      - name: Build project (without tests)
        run: ./gradlew clean build -x test

      - name: Run tests
        run: ./gradlew test jacocoTestReport

      - name: Cache SonarQube packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: SonarQube analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        run: ./gradlew sonar -Dsonar.login=$SONAR_TOKEN

      - name: Upload JAR artifacts
        uses: actions/upload-artifact@v4
        with:
          name: app-jar
          path: build/libs/*.jar
          retention-days: 7


  cd-main:
    name: CD - Main
    needs: ci-build-test-analysis-upload
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/tasks/USR-0')

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: app-jar

      - name: Login to Docker Hub
        uses: docker/login-action@343f7c4344506bcbf9b4de18042ae17996df046d

        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker images
        env:
          DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
          COMMIT_SHA: ${{ github.sha }}
        run: |
          docker build \
            -t $DOCKER_USERNAME/userservice:main-latest \
            -t $DOCKER_USERNAME/userservice:$COMMIT_SHA \
            -f Dockerfile .
          
          docker push $DOCKER_USERNAME/userservice:main-latest
          docker push $DOCKER_USERNAME/userservice:$COMMIT_SHA



  
